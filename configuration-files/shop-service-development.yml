server:
  port: 10000
logging:
  level:
    pl.training: DEBUG
feign:
  client:
    config:
      default:
        logger-level: BASIC
api:
  products: http://WAREHOUSE-SERVICE/products/
  payments: http://PAYMENTS-SERVICE/payments/
