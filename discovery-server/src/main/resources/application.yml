spring:
  application:
    name: discovery-server
  profiles:
    active: development
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      label: 003-Microservice-extraction
